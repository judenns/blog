import { defineConfig } from "vite";
// Import framework-specific plugins if needed, e.g.:
// import react from '@vitejs/plugin-react';

export default defineConfig({
    plugins: [
        // react()
    ],
});
